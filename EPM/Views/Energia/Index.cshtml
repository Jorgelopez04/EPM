@model IEnumerable<EPM.tb_Energia>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MetaAhorroEnergia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConsumoActualEnergia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_Cliente.Nombres)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MetaAhorroEnergia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConsumoActualEnergia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tb_Cliente.Nombres)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
